name: Deploy

# Set-up prior to first run:
# 1. Create 'production' environment: https://github.com/musescore/MuseScore/settings/environments
# 2. Enable 'required reviewers': MuseScore Editor Team

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Mode: test, alpha, beta, rc, stable"
        required: true
        default: "test"

defaults:
  run:
    shell: bash

jobs:
  create-builds:
    runs-on: ubuntu-latest
    environment: production # requires approval
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      # create builds for all platforms
      # update backend

  # smoke test, approve all builds

  update-youtube-playlists:
    needs: create-builds
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://github.com
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      # create release tag
      # create release page

  # check playlists

  create-draft-release:
    needs: update-youtube-playlists
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://github.com
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      # create release tag
      # create release page

  # check release page and publish, wait for day, if there is no critical issues, then continue

  notify-users:
    needs: create-draft-release
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://github.com
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
