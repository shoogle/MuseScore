# These SVGs already optimized, so just copy to build folder
file(COPY musescore-icon-round.svg DESTINATION .)
file(COPY musescore-icon-square.svg DESTINATION .)

# rasterize(musescore-icon-round.svg)
